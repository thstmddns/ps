```
import sys

N, M = map(int, sys.stdin.readline().split())
lis = [0] * N
for _ in range(M):
    i, j, k = map(int, sys.stdin.readline().split())
    for l in range(i, j + 1):
        lis[l-1] = k
print(*lis)
```